/*
 * The interpreter for brainfuck, take a list of instructions
 * and executes them
 */

// return 0 on succes interpretation, and -1 on error
int interpret(struct instlist* list){

  char *pointer = malloc(2048); // malloc 2 mbyte

  free (pointer);
  return 0;

}
